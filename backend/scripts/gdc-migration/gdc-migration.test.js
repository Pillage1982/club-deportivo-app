'use strict';

const test = require('node:test'); const assert = require('node:assert/strict');
const { normalizeRut, validateRut } = require('./rut'); const { hashMovement, compareMembers, reconcileAttendanceRuts } = require('./core');
const { groupEvents, calculateScores } = require('./cli'); const { dateOnly, parseAttendanceDate, assignRepresentativeEventTimes, ATTENDANCE_CUTOFF, ATTENDANCE_DATE_CORRECTIONS } = require('./excel');
const { PERIODS, allocatePaymentsToQuotas } = require('./paymentAllocation');

test('normaliza y valida RUT chileno', () => { assert.equal(normalizeRut('12.345.678-5'), '12345678-5'); assert.equal(validateRut('12.345.678-5'), true); assert.equal(validateRut('12.345.678-4'), false); assert.equal(normalizeRut('22 518 710 k'), '22518710-K'); });
test('detecta duplicados por RUT y no por nombre', () => { const rows=compareMembers({db:[],members:[{rut:'12345678-5',nombre:'A'},{rut:'12345678-5',nombre:'B'}],finances:[],attendance:[],positions:[]}); assert.equal(rows[0].accion_recomendada,'REVISAR_DUPLICADO'); });
test('hash de movimiento es idempotente', () => { assert.equal(hashMovement(['a',1]),hashMovement(['a',1])); assert.notEqual(hashMovement(['a',1]),hashMovement(['a',2])); });
test('honorario y receso no generan pagos propuestos', () => { const people=[{rut:'12345678-5',es_honorario:true},{rut:'11111111-1',estado:'receso'}]; const payments=[]; assert.equal(calculateScores(people,[],payments).size,0); });
test('socio activo puede recibir puntaje de asistencia', () => { const scores=calculateScores([{rut:'12345678-5',estado:'activo'}],[{rut:'12345678-5',fecha:'2025-09-27'}],[]); assert.equal(scores.get('12345678-5'),5); });
test('corte incluye 10-10-2025 y excluye eventos previos', () => { const events=groupEvents([{fecha:'2025-10-09',fecha_hora:'2025-10-09 10:00:00',rut:'1',evento:'Anterior',incluido:false},{fecha:ATTENDANCE_CUTOFF,fecha_hora:'2025-10-10 10:00:00',rut:'2',evento:'Evento pendiente',incluido:true}]); assert.equal(events[0].incluido_en_temporada,false); assert.equal(events[1].incluido_en_temporada,true); });
test('detecta último evento real de junio por orden', () => { const events=groupEvents([{fecha:'2026-06-01',fecha_hora:'2026-06-01 10:00:00',rut:'1',evento:'A',incluido:true},{fecha:'2026-06-29',fecha_hora:'2026-06-29 20:00:00',rut:'2',evento:'B',incluido:true}]); assert.equal(events.at(-1).fecha_ultima_marcacion,'2026-06-29 20:00:00'); });
test('pagos parciales no se convierten en texto', () => { const scores=calculateScores([],[{rut:'12345678-5',fecha:'2026-01-01'}],[{rut:'12345678-5',anio:2026,mes:1,monto:5000}]); assert.equal(scores.get('12345678-5'),10); });
test('fecha Excel conserva el día sin desplazamiento de zona horaria', () => { assert.equal(dateOnly(new Date('2025-09-27T00:00:00.000Z')),'2025-09-27'); });
test('acepta fecha de asistencia escrita como texto dd/mm/aaaa', () => { assert.equal(parseAttendanceDate('   27/06/2026'),'2026-06-27'); });
test('no confunde un RUT numérico con fecha u hora', () => { assert.equal(parseAttendanceDate(203052103),null); });
test('eventos de la misma fecha conservan bloques separados', () => { const events=groupEvents([{evento_origen_id:'A:1',fecha:'2025-12-08',fecha_hora:'2025-12-08',rut:'1',evento:'Uno',incluido:true},{evento_origen_id:'A:2',fecha:'2025-12-08',fecha_hora:'2025-12-08',rut:'1',evento:'Dos',incluido:true}]); assert.equal(events.length,2); });
test('corrige de forma explícita el año del bloque de diciembre', () => { assert.deepEqual(ATTENDANCE_DATE_CORRECTIONS.get('Asistencias:4748'),{date:'2025-12-20',reason:'Año confirmado por revisión manual; original 20-12-2024'}); });
test('limita correcciones visuales a los bloques confirmados', () => { assert.equal(ATTENDANCE_DATE_CORRECTIONS.get('Asistencias:4955').date,'2026-04-10'); assert.equal(ATTENDANCE_DATE_CORRECTIONS.get('Asistencias:5172').date,'2026-05-09'); });
test('corrige RUT de asistencia sólo con candidato maestro único', () => { const result=reconcileAttendanceRuts([{rut_valido:false,valor_original:'12345679-5',nombre:'',incluido:true}],[{rut:'12345678-5',nombre:'Persona',source:'BD'}]); assert.equal(result.corrected[0].rut,'12345678-5'); });
test('asigna 15:00 a evento único en sábado y 18:00 en otro día', () => { const rows=assignRepresentativeEventTimes([{incluido:true,fecha:'2026-05-09',evento_origen_id:'A',fila_inicio_evento:1,hora_disponible:false},{incluido:true,fecha:'2026-05-10',evento_origen_id:'B',fila_inicio_evento:2,hora_disponible:false}]); assert.equal(rows[0].hora,'15:00:00'); assert.equal(rows[1].hora,'18:00:00'); });
test('asigna intervalos de dos horas a eventos de la misma fecha', () => { const rows=assignRepresentativeEventTimes([{incluido:true,fecha:'2025-12-08',evento_origen_id:'A',fila_inicio_evento:1,hora_disponible:false},{incluido:true,fecha:'2025-12-08',evento_origen_id:'B',fila_inicio_evento:2,hora_disponible:false}]); assert.equal(rows[0].hora,'15:00:00'); assert.equal(rows[1].hora,'17:00:00'); });
test('distribución anual suma exactamente 120000 en doce cuotas mensuales', () => { assert.equal(PERIODS.length,12); assert.equal(PERIODS.reduce((sum,period)=>sum+period.amount,0),120000); assert.equal(PERIODS.every(period=>period.amount===10000),true); assert.deepEqual([PERIODS[0].year,PERIODS[0].month],[2025,10]); assert.deepEqual([PERIODS.at(-1).year,PERIODS.at(-1).month],[2026,9]); });
test('pago de 20000 en enero cubre octubre y noviembre como atrasado', () => { const result=allocatePaymentsToQuotas([{rut:'12345678-5',monto:20000,anio:2026,mes:1,fila_origen:1,referencia_externa:'x'}],[{rut:'12345678-5',rut_valido:true,nombre:'Ejemplo',estado:'Activo'}]); const rows=result.allocations; assert.deepEqual(rows.map(row=>[row.cuota_periodo,row.monto_asignado,row.oportunidad]),[['2025-10',10000,'atrasado'],['2025-11',10000,'atrasado']]); assert.equal(result.quotaStatus.find(row=>row.cuota_periodo==='2025-11').saldo_cuota,0); });
